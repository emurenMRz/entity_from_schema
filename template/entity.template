import jakarta.persistence.Entity;
import jakarta.persistence.Column;
import org.springframework.data.annotation.Id;
import lombok.Data;

import java.sql.*;

/**
 * Entity for $target_schema
 */
@Entity
@Table(name = "$table_name")$id_class
@Data
public class $entity_name {$columns}
